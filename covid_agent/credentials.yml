rasa:
  url: "http://localhost:5002/api"

rest:
  # you don't need to provide anything here - this channel doesn't
  # require any credentials

telegram:
  access_token: "your_telegram_bot_token"
  verify: "your_bot"
  webhook_url: "http://<host>:<port>/webhooks/telegram/webhook"